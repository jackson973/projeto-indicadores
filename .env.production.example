# ==========================================
# SERVIDOR - apps/server/.env
# ==========================================

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=indicadores
DB_USER=indicadores_user
DB_PASSWORD=TROQUE_POR_SENHA_SUPER_SEGURA

# JWT Secret (gere uma chave aleatória segura)
# Exemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=TROQUE_POR_CHAVE_JWT_SUPER_SECRETA_MINIMO_32_CARACTERES

# Server
PORT=4000
NODE_ENV=production

# App URL (seu domínio ou IP do servidor)
APP_URL=https://seudominio.com
# ou sem domínio:
# APP_URL=http://SEU_IP_SERVIDOR

# Email Configuration (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seuemail@gmail.com
SMTP_PASS=sua_senha_de_app_do_gmail
SMTP_FROM=Sistema Indicadores <seuemail@gmail.com>

# ==========================================
# CLIENTE - apps/client/.env.production
# ==========================================

# API URL (onde o frontend vai buscar dados)
VITE_API_URL=https://seudominio.com
# ou sem domínio:
# VITE_API_URL=http://SEU_IP_SERVIDOR

# ==========================================
# NOTAS IMPORTANTES
# ==========================================

# 1. Gmail - Senha de App:
#    - Acesse: https://myaccount.google.com/apppasswords
#    - Crie uma senha de app específica para este projeto
#    - Use essa senha no SMTP_PASS

# 2. JWT Secret:
#    - Gerar chave segura:
#      node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    - Ou use: openssl rand -hex 32

# 3. Database Password:
#    - Use senha forte com letras, números e símbolos
#    - Não use a senha padrão deste exemplo!

# 4. APP_URL e VITE_API_URL:
#    - Se usar domínio: https://seudominio.com
#    - Se usar apenas IP: http://SEU_IP (sem HTTPS)
#    - Devem ser iguais se o Nginx estiver servindo tudo
