{
  "name": "upseller-dashboard-server",
  "version": "0.1.0",
  "main": "src/index.js",
  "scripts": {
    "dev": "node src/index.js",
    "test": "node --test",
    "db:start": "cd ../.. && docker-compose up -d postgres",
    "db:stop": "cd ../.. && docker-compose stop postgres",
    "db:logs": "cd ../.. && docker-compose logs -f postgres",
    "db:shell": "docker exec -it projeto-indicadores-db psql -U indicadores_user -d indicadores"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.0",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.1",
    "pg": "^8.11.3",
    "xlsx": "^0.18.5"
  }
}
